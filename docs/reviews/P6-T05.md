# P6-T05 Release Preparation

Date: 2026-09-12
Status: Local configuration and documentation complete; signed install/upgrade
verification remains pending.

## Delivered

- Release shrinking and resource shrinking are enabled and `assembleRelease`
  passes.
- Signing is configured only through external Gradle properties; no keystore or
  secret is stored in source control.
- `README.md`, `docs/PRIVACY_POLICY.md`, `docs/RELEASE_NOTES.md`, and
  `docs/RELEASE_OPERATIONS.md` document supported behavior, limits, rollback,
  and privacy-safe monitoring.

## Remaining Evidence

Supply the production signing identity outside source control, build a signed
artifact, and execute fresh-install/upgrade verification during the final device
campaign. The local unsigned APK is not a release-ready distribution artifact.

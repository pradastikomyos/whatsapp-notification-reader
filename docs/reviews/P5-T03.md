# P5-T03 Reader Settings

> Superseded by `docs/adr/ADR-013-group-reader-removal.md`: group policy was
> removed. The current screen contains direct-message settings only.

Date: 2026-09-12
Status: Route and controller passed locally; navigation integration remains open.

- `ReaderSettingsRoute` persists only private-message reading, the approved
  three-mode group policy, and sender/group announcement through DataStore.
- Indonesian locale and the bounded queue policy are displayed as fixed v1
  facts. No unsupported locale or queue control was introduced.
- Settings write failures are visible and cancellation is propagated.

Focused settings tests, full `testDebugUnitTest`, `lintDebug`, and
`assembleDebug` passed. Phase 4 device-gate debt remains open.

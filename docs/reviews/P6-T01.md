# P6-T01 Automated Suite

Date: 2026-09-12
Status: Local suite passed; instrumented execution remains pending a configured
emulator or the final device campaign.

Passed locally:

```powershell
.\gradlew.bat --no-daemon --rerun-tasks testDebugUnitTest --tests "com.ridenotify.app.wa_reader.speech.SpeechCoordinatorTest" --tests "com.ridenotify.app.wa_reader.ui.*"
.\gradlew.bat --no-daemon testDebugUnitTest lintDebug assembleDebug assembleRelease
git diff --check
```

There is currently no `app/src/androidTest` suite and no configured emulator.
`connectedDebugAndroidTest` and physical install execution are therefore not
reported as pass. Add an instrumented regression for any defect found during
the final campaign when its scenario cannot be covered by the existing unit or
Robolectric suites.

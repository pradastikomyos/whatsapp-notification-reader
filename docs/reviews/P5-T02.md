# P5-T02 Home Controls

Date: 2026-09-12
Status: Route and controller passed locally; navigation integration remains open.

- `HomeRoute` updates reader enablement and speech rate directly through the
  shared DataStore repository and uses the same riding controller as P5-T05.
- Test speech runs the fixed synthetic phrase through `SpeechCoordinator`, so it
  uses the foreground playback, audio-focus, and one-active-utterance path.
- The UI reports pending, speaking, successful completion, or failure. Locale
  detection alone never appears as a successful playback test.
- A reader/riding transition that flushes a pending test marks the test failed
  rather than leaving it pending.

Focused Home/coordinator tests, full `testDebugUnitTest`, `lintDebug`, and
`assembleDebug` passed. Phase 4 device-gate debt remains open.
